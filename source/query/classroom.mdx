---
title: 학급 QUERY
description: 학급 서비스 Query
---

import GraphiQL from '../../src/components/GraphiQL'
import { Link } from 'gatsby'


### `classroom`
`Type` : <Link to="/object/classroom/#Classroom/">Classroom</Link>

> 학급 상세정보

**Arguments**

| Name | Description |
| -------- | -------- | 
| where  (<Link to="/input/classroom/#ClassroomWhere">ClassroomWhere</Link>)  | 학급 Where  | 

<GraphiQL
  query="
    query classroom($where: ClassroomWhere) {
      classroom(where: $where) {
        id
        title
        ownerId
        ownerName
        cLesson
        cLessonDoing
        openType
        gradeType
        createdAt
        updatedAt
        mainImg {
            url
        }
      }
    }
  "
  authButtons={{
    login: ['kaye@luxrobo.com', 'qwer1234'],
  }}
  variables={{
    where: {
      id: "1946889872"
    }
  }}
/>



### `classroomConnection`
`Type` : <Link to="/object/classroom/#ClassroomConnection/">ClassroomConnection</Link>

> 학급 리스트 (검색을 통해 제공)

**Arguments**

| Name | Description |
| -------- | -------- | 
| first (Int!)  | 요청개수   | 
| after (String) | Cursor다음 페이지 String | 
| offset (Int) | 다음페이지 offset limit정보 |
| orderBy (<Link to="/input/classroom/#ClassroomConnectionOrder">ClassroomConnectionOrder</Link>)  | 커리큘럼 Orderby  | 
| where  (<Link to="/input/classroom/#ClassroomConnectionWhere">ClassroomConnectionWhere</Link>)  |  Where  | 

<GraphiQL
  query="
    query classroomConnection($first: Int!, $after: String, $offset: Int, $orderBy: ClassroomConnectionOrder, $where: ClassroomConnectionWhere) {
      classroomConnection(first: $first, after: $after, offset: $offset, orderBy: $orderBy, where: $where) {
        totalCount
        pageInfo {
            endCursor
            startCursor
            hasNextPage
        }
        nodes {
          id
          title
          ownerId
          ownerName
          cLesson
          cLessonDoing
          openType
          gradeType
          createdAt
          updatedAt
          mainImg {
              url
          }
        }
      }
    }
  "
  authButtons={{
    login: ['kaye@luxrobo.com', 'qwer1234'],
  }}
  variables={{
    first: 10,
    after: "",
    offset: 0,
    orderBy: {
      field: "ID"
    },
    where: {
      filter: ""
    }
  }}
/>


### `classroomNotice`
`Type` : <Link to="/object/classroom/#ClassroomNotice/">ClassroomNotice</Link>

> 학급공지 상세정보

**Arguments**

| Name | Description |
| -------- | -------- | 
| where  (<Link to="/input/classroom/#ClassroomNoticeWhere">ClassroomNoticeWhere</Link>)  | 학급공지 Where  | 

<GraphiQL
  query="
    query classroomNotice($where: ClassroomNoticeWhere!) {
      classroomNotice(where: $where) {
        id
        title
        content
        createdAt
        updatedAt
      }
    }
  "
  authButtons={{
    login: ['kaye@luxrobo.com', 'qwer1234'],
  }}
  variables={{
    where: {
      id: "1",
      classroomId: "1946889872"
    }
  }}
/>

### `classroomNoticeConnection`
`Type` : <Link to="/object/classroom/#ClassroomNoticeConnection/">ClassroomNoticeConnection</Link>

> 학급 공지 리스트 (검색을 통해 제공)

**Arguments**

| Name | Description |
| -------- | -------- | 
| first (Int!)  | 요청개수   | 
| after (String) | Cursor다음 페이지 String | 
| offset (Int) | 다음페이지 offset limit정보 |
| orderBy (<Link to="/input/classroom/#ClassroomNoticeConnectionOrder">ClassroomConnectionOrder</Link>)  |  Orderby  | 
| where  (<Link to="/input/classroom/#ClassroomNoticeConnectionWhere">ClassroomConnectionWhere!</Link>)  |  Where  | 

<GraphiQL
  query="
    query classroomNoticeConnection($first: Int!, $after: String, $offset: Int, $orderBy: ClassroomNoticeConnectionOrder, $where: ClassroomNoticeConnectionWhere!) {
      classroomNoticeConnection(first: $first, after: $after, offset: $offset, orderBy: $orderBy, where: $where) {
        totalCount
        pageInfo {
            endCursor
            startCursor
            hasNextPage
        }
        nodes {
          id
            title
            content
            createdAt
            updatedAt
        }
      }
    }
  "
  authButtons={{
    login: ['kaye@luxrobo.com', 'qwer1234'],
  }}
  variables={{
    first: 10,
    after: "",
    offset: 0,
    orderBy: {
      field: "ID"
    },
    where: {
      classroomId: "1946889872"
    }
  }}
/>





### `classroomHomework`
`Type` : <Link to="/object/classroom/#ClassroomHomework/">ClassroomHomework</Link>

> 학급 숙제 상세정보

**Arguments**

| Name | Description |
| -------- | -------- | 
| where  (<Link to="/input/classroom/#ClassroomHomeworkWhere">ClassroomHomeworkWhere</Link>)  | 학급숙제 Where  | 

<GraphiQL
  query="
    query classroomHomework($where: ClassroomHomeworkWhere!) {
      classroomHomework(where: $where) {
        id
        lessonId
        lessonName
        classroomId
        isSubmission
        userId
        userName
        progress {
          total
          progress
          progressRate
        }
        title
        content
        expireDate
        createdAt
        updatedAt
      }
    }
  "
  authButtons={{
    login: ['kaye@luxrobo.com', 'qwer1234'],
  }}
  variables={{
    where: {
      id: "1",
      classroomId: "1946889872"
    }
  }}
/>

### `classroomHomeworkConnection`
`Type` : <Link to="/object/classroom/#ClassroomHomeworkConnection/">ClassroomHomeworkConnection</Link>

> 학급 숙제 리스트 (검색을 통해 제공)

**Arguments**

| Name | Description |
| -------- | -------- | 
| first (Int!)  | 요청개수   | 
| after (String) | Cursor다음 페이지 String | 
| offset (Int) | 다음페이지 offset limit정보 |
| orderBy (<Link to="/input/classroom/#ClassroomHomeworkConnectionOrder">ClassroomHomeworkConnectionOrder</Link>)  |  Orderby  | 
| where  (<Link to="/input/classroom/#ClassroomHomeworkConnectionWhere">ClassroomHomeworkConnectionWhere!</Link>)  |  Where  | 

<GraphiQL
  query="
    query classroomHomeworkConnection($first: Int!, $after: String, $offset: Int, $orderBy: ClassroomHomeworkConnectionOrder, $where: ClassroomHomeworkConnectionWhere!) {
      classroomHomeworkConnection(first: $first, after: $after, offset: $offset, orderBy: $orderBy, where: $where) {
        totalCount
        pageInfo {
            endCursor
            startCursor
            hasNextPage
        }
        nodes {
          id
          lessonId
          lessonName
          classroomId
          isSubmission
          userId
          userName
          progress {
            total
            progress
            progressRate
          }
          title
          content
          expireDate
          createdAt
          updatedAt
        }
      }
    }
  "
  authButtons={{
    classroom_login: ['정성균', '1111'],
  }}
  variables={{
    first: 10,
    after: "",
    offset: 0,
    orderBy: {
      field: "ID"
    },
    where: {
      classroomId: "2854540399"
    }
  }}
/>

### `classroomHomeworkGroupConnection`
`Type` : <Link to="/object/classroom/#ClassroomHomeworkGroupConnection">ClassroomHomeworkGroupConnection</Link>

> 학급 숙제 그룹

**Arguments**

| Name | Description |
| -------- | -------- | 
| first (Int!)  | 요청개수   | 
| after (String) | 다음날짜 | 
| orderBy (<Link to="/input/classroom/#ClassroomHomeworkGroupConnectionOrder">ClassroomHomeworkGroupConnectionOrder</Link>)  |  Orderby  | 
| where  (<Link to="/input/classroom/#ClassroomHomeworkGroupConnectionWhere">ClassroomHomeworkGroupConnectionWhere!</Link>)  |  Where  | 

<GraphiQL
  query="
    query classroomHomeworkGroupConnection($first: Int!, $after: String, $orderBy: ClassroomHomeworkGroupConnectionOrder, $where: ClassroomHomeworkGroupConnectionWhere!) {
      classroomHomeworkGroupConnection(first: $first, after: $after, orderBy: $orderBy, where: $where) {
        group {
          date
          nodes {
            id
            lessonId
            lessonName
            classroomId
            isSubmission
            userId
            userName
            progress {
              total
              progress
              progressRate
            }
            title
            content
            expireDate
            createdAt
            updatedAt
          }
        }
      }
    }
  "
  authButtons={{
    classroom_login: ['정성균', '1111'],
  }}
  variables={{
    first: 3,
    after: "",
    orderBy: {
      direction: "DESC"
    },
    where: {
      classroomId: "2854540399"
    }
  }}
/>



### `classroomStudent`
`Type` : <Link to="/object/classroom/#ClassroomStudent/">ClassroomStudent</Link>

> 학급 학생 상세정보

**Arguments**

| Name | Description |
| -------- | -------- | 
| where  (<Link to="/input/classroom/#ClassroomStudentWhere">ClassroomStudentWhere</Link>)  | 학급학생 Where  | 

<GraphiQL
  query="
    query classroomStudent($where: ClassroomStudentWhere!) {
      classroomStudent(where: $where) {
        id
        classroomId
        name
        userId
        password
        cAttendance
        shortWord
        lastAccessTime
        createdAt
        updatedAt
      }
    }
  "
  authButtons={{
    classroom_login: ['정성균', '1111'],
  }}
  variables={{
    where: {
      userId: "20",
      classroomId: "2854540399"
    }
  }}
/>

### `classroomStudentConnection`
`Type` : <Link to="/object/classroom/#ClassroomStudentConnection/">ClassroomStudentConnection</Link>

> 학급 학생 리스트

**Arguments**

| Name | Description |
| -------- | -------- | 
| first (Int!)  | 요청개수   | 
| after (String) | Cursor다음 페이지 String | 
| offset (Int) | 다음페이지 offset limit정보 |
| orderBy (<Link to="/input/classroom/#ClassroomStudentConnectionOrder">ClassroomStudentConnectionOrder</Link>)  |  Orderby  | 
| where  (<Link to="/input/classroom/#ClassroomStudentConnectionWhere">ClassroomStudentConnectionWhere!</Link>)  |  Where  | 

<GraphiQL
  query="
    query classroomStudentConnection($first: Int!, $after: String, $offset: Int, $orderBy: ClassroomStudentConnectionOrder, $where: ClassroomStudentConnectionWhere!) {
      classroomStudentConnection(first: $first, after: $after, offset: $offset, orderBy: $orderBy, where: $where) {
        totalCount
        pageInfo {
            endCursor
            startCursor
            hasNextPage
        }
        nodes {
          id
          classroomId
          userId
          name
          password
          cAttendance
          shortWord
          lastAccessTime
          createdAt
          updatedAt
        }
      }
    }
  "
  authButtons={{
    login: ['kaye@luxrobo.com', 'qwer1234'],
  }}
  variables={{
    first: 10,
    after: "",
    offset: 0,
    orderBy: {
      field: "ID"
    },
    where: {
      classroomId: "1946889872"
    }
  }}
/>



### `classroomStudentLessonDashboardConnection`
`Type` : <Link to="/object/classroom/#ClassroomStudentLessonDashboard/">ClassroomStudentLessonDashboard</Link>

> 학급 학생 Dashboard 리스트

**Arguments**

| Name | Description |
| -------- | -------- | 
| first (Int!)  | 요청개수   | 
| after (String) | Cursor다음 페이지 String | 
| offset (Int) | 다음페이지 offset limit정보 |
| orderBy (<Link to="/input/classroom/#ClassroomStudentLessonDashboardConnectionOrder">ClassroomStudentLessonDashboardConnectionOrder</Link>)  |  Orderby  | 
| where  (<Link to="/input/classroom/#ClassroomStudentLessonDashboardConnectionWhere">ClassroomStudentLessonDashboardConnectionWhere!</Link>)  |  Where  | 

<GraphiQL
  query="
    query classroomStudentLessonDashboardConnection($first: Int!, $after: String, $offset: Int, $orderBy: ClassroomStudentLessonDashboardConnectionOrder, $where: ClassroomStudentLessonDashboardConnectionWhere!) {
      classroomStudentLessonDashboardConnection(first: $first, after: $after, offset: $offset, orderBy: $orderBy, where: $where) {
        totalCount
        pageInfo {
            endCursor
            startCursor
            hasNextPage
        }
        nodes {
          id
          classroomId
          lessonId
          lessonName
          userId
          userName
          progress {
            total
            progress
            progressRate
          }
          works {
            id
            name
            userId
            code
            codingTemplate {
              idx
              codingType
              initCode
              questions
              hardwareTypes {
                id
              }
              answerCode
              answerImgs {
                url
              }
              type
            }
            contentId
            lessonId
            createdAt
            updatedAt
          }
          status
          learningTime
          createdAt
          updatedAt
        }
      }
    }
  "
  authButtons={{
    login: ['kaye', 'qwer1234'],
  }}
  variables={{
    first: 10,
    after: "",
    offset: 0,
    orderBy: {
      field: "ID"
    },
    where: {
      lessonId: "82835450",
      classroomId: "1946889872"
    }
  }}
/>
