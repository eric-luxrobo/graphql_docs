---
title: 계정관리 QUERY
description: 계정관리 서비스입니다. 
---

import GraphiQL from '../../src/components/GraphiQL'
import { Link } from 'gatsby'

### `accountMember`
`Type` : <Link to="/object/account/#AccountMember">AccountMember</Link>

> Role: `[관리자, 회원]` 회원정보 상세

**Arguments**

| Name | Description |
| -------- | -------- | 
| userId (String!)  | 회원가입 유저ID  | 


<GraphiQL
  query="
    query accountMember($where: AccountMemberWhere!) {
      accountMember(where: $where) {
        id
        userId
        userName
        userEmail
        gender
        provider
        accountCode
        accountCodeName
        memberCode
        memberCodeName
        adminCode
        adminCodeName
        createdAt
        deletedAt
      }
    }
  "
  authButtons={{
    login: ['kaye@luxrobo.com', 'qwer1234'],
  }}
  variables={{
    where: {
      memberId: 11,
    }
  }}
/>

### `accountMemberConnection`
`Type` : <Link to="/object/account/#AccountMemberConnection/">AccountMemberConnection</Link>

> Role: `[관리자]` 회원정보 리스트를 보여줍니다. 

**Arguments**

| Name | Description |
| -------- | -------- | 
| first (Int!)  | 요청개수   | 
| after (String) | Cursor다음 페이지 String | 
| where  (<Link to="/input/account/#AccountMemberConnectionWhere">AccountMemberConnectionWhere</Link>)  | 회원 목록 Where  | 
| orderBy (<Link to="/input/account/#AccountMemberConnectionOrder">AccountMemberConnectionOrder</Link>)  | 회원 목록 Orderby  | 

<GraphiQL
  query="
    query accountMemberConnection(
      $first: Int!, 
      $after: String, 
      $where: AccountMemberConnectionWhere, 
      $orderBy: AccountMemberConnectionOrder
    ) {
      accountMemberConnection(first: $first, after: $after, where: $where, orderBy: $orderBy) {
        totalCount
        pageInfo {
          startCursor
          endCursor
          hasNextPage
        }
        nodes {
          id
          userId
          userName
          userEmail
          gender
          provider
          accountCode
          accountCodeName
          memberCode
          memberCodeName
          adminCode
          adminCodeName
          createdAt
          deletedAt
        }
      }
    }
  "
  authButtons={{
    login: ['kaye@luxrobo.com', 'qwer1234'],
  }}
  variables={{
    first: 10,
    after: '',
  }}
/>

### `accountSchoolConnection` 
`Type` : <Link to="/object/account/#AccountSchoolConnection/">AccountSchoolConnection</Link>

> 학교 정보 리스트

**Arguments**

| Name | Description |
| -------- | -------- | 
| first (Int!)  | 요청개수   | 
| after (String) | Cursor다음 페이지 String | 
| where  (<Link to="/input/account/#AccountSchoolConnectionWhere">AccountSchoolConnectionWhere</Link>)  | 회원 목록 Where  | 
| orderBy (<Link to="/input/account/#AccountSchoolConnectionOrder">AccountSchoolConnectionOrder</Link>)  | 회원 목록 Orderby  | 

<GraphiQL
  query="
    query accountSchoolConnection(
      $first: Int!, 
      $after: String!, 
      $where: AccountSchoolConnectionWhere, 
      $orderBy: AccountSchoolConnectionOrder
    ) {
      accountSchoolConnection(first: $first, after: $after, where: $where, orderBy: $orderBy) {
        totalCount
        pageInfo {
          startCursor
          endCursor
          hasNextPage
        }
        nodes {
          id
          gubunName
          schoolName
          schoolLink
          schoolAddress
          schoolRegion
        }
      }
    }
  "
  variables={{
    first: 10,
    after: "",
  }}
/>

### `accountCodeConnection` 
`Type` : <Link to="/object/account/#AccountCodeConnection/">AccountCodeConnection</Link>

> 계정 코드정보 리스트

**Arguments**

| Name | Description |
| -------- | -------- | 
| where  (<Link to="/input/account/#AccountCodeConnectionWhere">AccountCodeConnectionWhere</Link>)  | 계정 코드정보 Where  | 

<GraphiQL
  query="
    query accountCodeConnection($where: AccountCodeConnectionWhere!) {
      accountCodeConnection(where: $where) {
        groupCode
        groupName
        description
        nodes {
          code
          name
          description
        }
      }
    }
  "
  variables={{
    where: {
      code: 10300,
    }
  }}
/>


### `accountIsDuplicateUserId` 
`Type` : (int) 1: 존재 0: 없음 

> 유저ID 중복체크

**Arguments**

| Name | Description |
| -------- | -------- | 
| where  (<Link to="/input/account/#AccountIsDuplicateUserIdWhere">AccountIsDuplicateUserIdWhere</Link>)  | 유저ID 중복체크 Where  | 

<GraphiQL
  query="
    query accountIsDuplicateUserId($where: AccountIsDuplicateUserIdWhere!) {
      accountIsDuplicateUserId(where: $where)
    }
  "
  variables={{
    where: {
      userId: "tech@luxrobo.com"
    }
  }}
/>

### `accountIsDuplicateUserEmail` 
`Type` : (int) 1: 존재 0: 없음 

> 이메일 중복체크

**Arguments**

| Name | Description |
| -------- | -------- | 
| where  (<Link to="/input/account/#AccountIsDuplicateUserEmailWhere">AccountIsDuplicateUserEmailWhere</Link>)  | 유저 이메일 중복체크 Where  | 

<GraphiQL
  query="
    query accountIsDuplicateUserEmail($where: AccountIsDuplicateUserEmailWhere!) {
      accountIsDuplicateUserEmail(where: $where)
    }
  "
  variables={{
    where: {
      provider: "LOCALS",
      userEmail: "tech@luxrobo.com"
    }
  }}
/>

