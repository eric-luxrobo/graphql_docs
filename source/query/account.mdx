---
title: Account Query
description: 로그인/로그아웃등 사용자 계정 서비스
---

import GraphiQL from '../../src/components/GraphiQL'
import Note from '../../src/components/Note'

# members 
> 멤버 리스트

**Parameter**

| key | 설명 | 타입 | 필수요청 |
| -------- | -------- | -------- | -------- | 
| page     | 페이즈값 디폴트 1   | int | true |
| size | 리스트 사이즈 개수 | int | true |
| field | 필드 요청 | int | true |
| word | 필터링 단어 | int | true |

<GraphiQL
  query="
    query members($page: Int!, $size: Int!) {
      members(page: $page, size: $size) {
        total_cnt
        page_cnt
        datas {
          id
          user_id
          user_name
          created_at
          updated_at
        }
      }
    }
  "
  variables={{
    page: 1,
    size: 10,
  }}
/>

# member
> 회원 정보

**Parameter**

| key | 설명 | 타입 | 필수요청 |
| -------- | -------- | -------- | -------- | 
| userId     | 유저 ID   | string | true |


<GraphiQL
  query="
    query member($userId: String!) {
      member(userId: $userId) {
        id
        user_id
        user_name
        created_at
        updated_at
      }
    }
  "
  variables={{
    userId: "howto38",
  }}
/>
