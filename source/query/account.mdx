---
title: account QUERY
description: 계정관리 서비스입니다. 
---

import GraphiQL from '../../src/components/GraphiQL'
import { Link } from 'gatsby'

### `accountMemberConnection`
`Type` : <Link to="/type/account/#AccountMemberConnection/">AccountMemberConnection</Link>

> Role: `[관리자]` 회원정보 리스트를 보여줍니다. 

**Arguments**

| Name | Description |
| -------- | -------- | 
| page (Int!)  | 페이즈값 디폴트 1  | 
| size (Int!) | 리스트 사이즈 개수 | 
| field (Int) | 필드 요청 |
| word (Int) | 필터링 단어 | 

<GraphiQL
  query="
    query accountMemberConnection($page: Int!, $size: Int!) {
      accountMemberConnection(page: $page, size: $size) {
        totalCnt
        pageCnt
        nodes {
          id
          userId
          userName
          createdAt
        }
      }
    }
  "
  variables={{
    page: 1,
    size: 10,
  }}
/>

### `accountMember`
`Type` : <Link to="/type/account/#AccountMember/">AccountMember</Link>

> Role: `[관리자, 회원]` 회원정보 상세

**Arguments**

| Name | Description |
| -------- | -------- | 
| userId (String!)  | 회원가입 유저ID  | 


<GraphiQL
  query="
    query accountMember($userId: String!) {
      accountMember(userId: $userId) {
        id
        userId
        userName
        createdAt
      }
    }
  "
  variables={{
    userId: "howto38",
  }}
/>

### `accountIsDuplicateUserId` 
`Type` : (int) 1: 존재 0: 없음 

> 유저ID 중복체크

**Arguments**

| Name | Description |
| -------- | -------- | 
| userId (String!)  | 회원가입 유저ID  | 

<GraphiQL
  query="
    query accountIsDuplicateUserId($userId: String!) {
      accountIsDuplicateUserId(userId: $userId)
    }
  "
  variables={{
    userId: "howto38"
  }}
/>

### `accountIsDuplicateUserEmail` 
`Type` : (int) 1: 존재 0: 없음 

> 이메일 중복체크

**Arguments**

| Name | Description |
| -------- | -------- | 
| userEmail (String)  | 회원가입 유저이메일  | 

<GraphiQL
  query="
    query accountIsDuplicateUserEmail($userEmail: String!) {
      accountIsDuplicateUserEmail(userEmail: $userEmail)
    }
  "
  variables={{
    userEmail: "howto38"
  }}
/>