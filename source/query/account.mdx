---
title: account QUERY
description: 계정관리 서비스입니다. 
---

import GraphiQL from '../../src/components/GraphiQL'
import { Link } from 'gatsby'

### `accountMember`
`Type` : <Link to="/object/account/#AccountMember">AccountMember</Link>

> Role: `[관리자, 회원]` 회원정보 상세

**Arguments**

| Name | Description |
| -------- | -------- | 
| userId (String!)  | 회원가입 유저ID  | 


<GraphiQL
  query="
    query accountMember($userId: String!) {
      accountMember(userId: $userId) {
        id
        userId
        userName
        createdAt
      }
    }
  "
  variables={{
    userId: "howto38",
  }}
/>

### `accountMemberConnection`
`Type` : <Link to="/object/account/#AccountMemberConnection/">AccountMemberConnection</Link>

> Role: `[관리자]` 회원정보 리스트를 보여줍니다. 

**Arguments**

| Name | Description |
| -------- | -------- | 
| page (Int!)  | 페이즈값 디폴트 1  | 
| size (Int!) | 리스트 사이즈 개수 | 
| field (String) | 필드 요청 |
| word (String) | 필터링 단어 | 

<GraphiQL
  query="
    query accountMemberConnection($page: Int!, $size: Int!) {
      accountMemberConnection(page: $page, size: $size) {
        totalCount
        pageCount
        nodes {
          id
          userId
          userName
          createdAt
        }
      }
    }
  "
  variables={{
    page: 1,
    size: 10,
  }}
/>

### `accountSchoolConnection` 
`Type` : <Link to="/object/account/#AccountSchoolConnection/">AccountSchoolConnection</Link>

> 학교 정보 리스트

**Arguments**

| Name | Description |
| -------- | -------- | 
| page (Int!)  | 페이즈값 디폴트 1  | 
| size (Int!) | 리스트 사이즈 개수 | 
| word (String) | 필터링 단어 | 

<GraphiQL
  query="
    query accountSchoolConnection($page: Int!, $size: Int!, $word: String) {
      accountSchoolConnection(page: $page, size: $size, word: $word) {
        totalCount
        pageCount
        nodes {
          id
          gubunName
          schoolName
          schoolLink
          schoolAddress
        }
      }
    }
  "
  variables={{
    page: 1,
    size: 10,
    word: "",
  }}
/>

### `accountCodeConnection` 
`Type` : <Link to="/object/account/#AccountCodeConnection/">AccountCodeConnection</Link>

> 계정 코드정보 리스트

**Arguments**

| Name | Description |
| -------- | -------- | 
| code (Int!)  | 코드정보  | 


<GraphiQL
  query="
    query accountCodeConnection($code: Int!) {
      accountCodeConnection(code: $code) {
        groupCode
        groupName
        description
        nodes {
          code
          name
          description
        }
      }
    }
  "
  variables={{
    code: 1,
  }}
/>


### `accountIsDuplicateUserId` 
`Type` : (int) 1: 존재 0: 없음 

> 유저ID 중복체크

**Arguments**

| Name | Description |
| -------- | -------- | 
| userId (String!)  | 회원가입 유저ID  | 

<GraphiQL
  query="
    query accountIsDuplicateUserId($userId: String!) {
      accountIsDuplicateUserId(userId: $userId)
    }
  "
  variables={{
    userId: "howto38"
  }}
/>

### `accountIsDuplicateUserEmail` 
`Type` : (int) 1: 존재 0: 없음 

> 이메일 중복체크

**Arguments**

| Name | Description |
| -------- | -------- | 
| provider (String!) | 서비스 프로바이더 정보
| userEmail (String!)  | 회원가입 유저이메일  | 

<GraphiQL
  query="
    query accountIsDuplicateUserEmail($provider: String!, $userEmail: String!) {
      accountIsDuplicateUserEmail(provider: $provider, userEmail: $userEmail)
    }
  "
  variables={{
    provider: "locals",
    userEmail: "howto38@gmail.com"
  }}
/>

### `accountFindUserId` 
`Type` : (String) 사용자 ID

> 이메일정보를 통해서 사용자 ID를 찾는다.

**Arguments**

| Name | Description |
| -------- | -------- | 
| userEmail (String!)  | 회원가입 유저이메일  | 

<GraphiQL
  query="
    query accountFindUserId($userEmail: String!) {
      accountFindUserId(userEmail: $userEmail)
    }
  "
  variables={{
    userEmail: "howto38@gmail.com"
  }}
/>