---
title: lesson QUERY
description: 커리큘럼 서비스 Query
---

import GraphiQL from '../../src/components/GraphiQL'
import { Link } from 'gatsby'

### `lesson`
`Type` : <Link to="/type/lesson/#Lesson/">Lesson</Link>

**Arguments**

| Name | Description |
| -------- | -------- | 
| where  (<Link to="/type/lesson/#LessonWhere">LessonWhere</Link>)  | 커리큘럼 Where  | 

<GraphiQL
  query="
    query lesson($where: LessonWhere!) {
      lesson(where: $where) {
        id
        title
        subTitle
        describe
        mainImgs {
          domain
          url
        }
        classTime
        userActivity {
          cLike
          cBookmark
        }
        levelType
        groupType
        elementJson
        lessonPlans {
          id
          title
          groups {
            id
            idx
            content 
          }
          template {
            id
            type
            describe
            img {
              url
            }
          }
        }
      }
    }
  "
  variables={{
    where: {
      id: "1011721328"
    }
  }}
/>



### `lessonConnection`
`Type` : <Link to="/type/lesson/#LessonConnection/">LessonConnection</Link>

**Arguments**

| Name | Description |
| -------- | -------- | 
| first (Int!)  | 요청개수   | 
| after (String) | Cursor다음 페이지 String | 
| offset (Int) | 다음페이지 offset limit정보 |
| orderBy (<Link to="/type/lesson/#LessonConnectionOrder">LessonConnectionOrder</Link>)  | 커리큘럼 Orderby  | 
| where  (<Link to="/type/lesson/#LessonConnectionWhere">LessonConnectionWhere</Link>)  | 커리큘럼 Where  | 

<GraphiQL
  query="
    query lessonConnection($first: Int!, $after: String, $orderBy: LessonConnectionOrder, $where: LessonConnectionWhere) {
      lessonConnection(first: $first, after: $after, orderBy: $orderBy, where: $where) {
        totalCount
        pageInfo {
            endCursor
            startCursor
            hasNextPage
        }
        nodes {
          id
          title
          subTitle
          describe
          mainImgs {
            domain
            url
          }
          classTime
          userActivity {
            cLike
            cBookmark
          }
          levelType
          groupType
          elementJson
          lessonPlans {
            id
            title
            groups {
              id
              idx
              content 
            }
            template {
              id
              type
              describe
              img {
                url
              }
            }
          }
        }
      }
    }
  "
  variables={{
    first: 10,
    after: "",
    orderBy: {
      field: "ID"
    },
    where: {
      filter: ""
    }
  }}
/>

### `lessonPlanTemplates`
`Type` : <Link to="/type/lesson/#LessonPlanTemplate/">LessonPlanTemplate</Link>

**Arguments**
없음 

<GraphiQL
  query="
    query lessonPlanTemplates {
      lessonPlanTemplates {
        id
        type
        describe
        img {
          url
        }
      }
    }
  "
/>