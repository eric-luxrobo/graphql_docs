---
title: 공지사항/FAQ QUERY
description: 공지사항/FAQ 서비스 Query
---

import GraphiQL from '../../src/components/GraphiQL'
import { Link } from 'gatsby'

### `boardCodeConnection`
> Board Code 리스트

`Type` : <Link to="/object/board/#BoardCodeConnection/">BoardCodeConnection</Link>

**Arguments**

| Name | Description |
| -------- | -------- | 
| where (<Link to="/input/board/#BoardCodeConnectionWhere">BoardCodeConnectionWhere</Link>)  |  Where  | 


<GraphiQL
  query="
  query boardCodeConnection(
    $where: BoardCodeConnectionWhere!
  ){
    boardCodeConnection(where: $where) {
      nodes {
        code
        name
        description
      }
      groupCode
      groupName
      description
    }
  }
  "
  variables={{
    where: {
      code: 20100
    }
  }}
/>

### `boardFaqConnection`
> FAQ 리스트


**Arguments**

| Name | Description |
| -------- | -------- | 
| first (Int!)  | 요청개수   | 
| after (String) | Cursor다음 페이지 String | 
| where (<Link to="/input/board/#BoardFaqConnectionWhere">BoardFaqConnectionWhere</Link>)  |  Where  | 
| orderBy (<Link to="/input/board/#BoardFaqConnectionOrder">BoardFaqConnectionOrder</Link>)  |  Orderby  | 

`Return Type`: String


<GraphiQL
  query="
  query boardFaqConnection(
    $first: Int!
    $after: String
    $where: BoardFaqConnectionWhere
    $orderBy: BoardFaqConnectionOrder
  ){
    boardFaqConnection(first: $first, after: $after, where: $where, orderBy: $orderBy) {
      nodes {
        id
        title
        content
        faqCode
        faqCodeName
        isView
        serviceType
        createdAt
      }
      totalCount
      pageInfo {
        startCursor
        endCursor
        hasNextPage
      }
    }
  }
  "
  variables={{
    first: 10,
    after: "",
  }}
/>

### `boardNoticeConnection`
> Notice 리스트

**Arguments**

| Name | Description |
| -------- | -------- | 
| first (Int!)  | 요청개수   | 
| after (String) | Cursor다음 페이지 String | 
| where (<Link to="/input/board/#BoardNoticeConnectionWhere">BoardNoticeConnectionWhere</Link>)  |  Where  | 
| orderBy (<Link to="/input/board/#BoardNoticeConnectionOrder">BoardNoticeConnectionOrder</Link>)  |  Orderby  | 

`Return Type`: String

<GraphiQL
  query="
  query boardNoticeConnection(
    $first: Int!
    $after: String
    $where: BoardNoticeConnectionWhere
    $orderBy: BoardNoticeConnectionOrder
  ){
    boardNoticeConnection(first: $first, after: $after, where: $where, orderBy: $orderBy) {
      nodes {
        id
        title
        content
        isTop
        isView
        serviceType
        createdAt
        images {
          id
          serviceId
          serviceName
          imageKey
          imageUrl
          idx
          createdAt
        }
      }
      totalCount
      pageInfo {
        startCursor
        endCursor
        hasNextPage
      }
    }
  }
  "
  variables={{
    first: 10,
    after: "",
  }}
/>