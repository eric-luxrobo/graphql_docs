---
title: account MUTATION
description: 계정관리 로그인/로그아웃등등 ...
---

import GraphiQL from '../../src/components/GraphiQL'
import { Link } from 'gatsby'

### `accountLogin` 
`Type` : (String) JWT 인증토큰정보 리턴 

> 로그인


`Input fields`
* input : <Link to="/type/account/#AccountLoginInput">AccountLoginInput</Link>

<GraphiQL
  query="
    mutation accountLogin($input: AccountLoginInput!) {
      accountLogin(input: $input)
    }
  "
  variables={{
    input: {
      userId: "howto38",
      password: "11111111"
    }
  }}
/>

### `accountOAuthLogin` 
`Type` : (String) JWT 인증토큰정보 리턴 

> 소셜로그인


`Input fields`
* input : <Link to="/type/account/#AccountOAuthLoginInput">AccountOAuthLoginInput</Link>

<GraphiQL
  query="
    mutation accountOAuthLogin($input: AccountOAuthLoginInput!) {
      accountOAuthLogin(input: $input)
    }
  "
  variables={{
    input: {
      userId: "howto38",
      userName: "walter",
      userEmail: "walter@daum.net",
      provider: "google",
      accountType: 1111,
      memberType: 2222,
      accessToken: "1212122",
      refreshToken: "we3e3432"
    }
  }}
/>

### `accountResetUserPassword`
`Type` : (String) 

> 소셜로그인

`Input fields`
* input : userEmail(String!)



<GraphiQL
  query="
  mutation accountResetUserPassword( $userEmail:String! ){
    accountResetUserPassword( userEmail: $userEmail )
  }
  "
  variables={{
    userEmail: ""
  }}
/>



### `authAccountUserEmail`
`Type` : (ID) 

> 이메일 인증하기

`Input fields`
* input : String

<GraphiQL
  query="
  mutation authAccountUserEmail( $userEmail:String! ){
    authAccountUserEmail( userEmail: $userEmail )
  }
  "
  variables={{
    userEmail: ""
  }}
/>


### `authCheckAccountUserEmail`
`Type` : (ID) 

> 이메일 인증여부 확인

`Input fields`
* input : <Link to="/type/account/#AuthCheckAccountUserEmailInput">AuthCheckAccountUserEmailInput</Link>

<GraphiQL
  query="
  mutation authCheckAccountUserEmail( $input:AuthCheckAccountUserEmailInput! ){
    authCheckAccountUserEmail( input: $input )
  }
  "
  variables={{
    input: {
      id: "1111",
      authCode:"11111111",
    },
  }}
/>

### `createAccountMember`
`Type` : (ID) 

> 회원가입

`Input fields`
* input : <Link to="/type/account/#CreateAccountMemberInput">CreateAccountMemberInput</Link>

<GraphiQL
  query="
  mutation createAccountMember( $input:CreateAccountMemberInput! ){
    createAccountMember( input: $input )
  }
  "
  variables={{
    input: {
      userId: "1111",
      password:"11111111",
      userName: "월터",
      userEmail: "walter@gmail.com",
      gender: "M",
      accountType: 1,
      memberType: 1,
    },
  }}
/>

