---
title: 계정관리 Mutation
description: 계정관리 로그인/로그아웃등등 ...
---

import GraphiQL from '../../src/components/GraphiQL'
import { Link } from 'gatsby'


### `authAccountUserEmail`
`Type` : (ID) 

> 이메일 인증 요청하기

`Input fields`
* input : <Link to="/input/account/#AuthAccountUserEmailInput">AuthAccountUserEmailInput</Link>

<GraphiQL
  query="
  mutation authAccountUserEmail( $input:AuthAccountUserEmailInput! ){
    authAccountUserEmail( input: $input )
  }
  "
  variables={{
    input: {
      userEmail: "walter@gmail.com"
    }
  }}
/>


### `authCheckAccountUserEmail`
`Type` : (ID) 

> 이메일 인증여부 확인

`Input fields`
* input : <Link to="/input/account/#AuthCheckAccountUserEmailInput">AuthCheckAccountUserEmailInput</Link>

<GraphiQL
  query="
  mutation authCheckAccountUserEmail( $input:AuthCheckAccountUserEmailInput! ){
    authCheckAccountUserEmail( input: $input )
  }
  "
  variables={{
    input: {
      id: "13",
      authCode:"123456",
    },
  }}
/>

### `createAccountMember`
`Type` : (ID) 

> 회원가입

`Input fields`
* input : <Link to="/input/account/#CreateAccountMemberInput">CreateAccountMemberInput</Link>

<GraphiQL
  query="
  mutation createAccountMember( $input:CreateAccountMemberInput! ){
    createAccountMember( input: $input )
  }
  "
  variables={{
    input: {
      userId: "tech@luxrobo.com",
      password:"1324qewr",
      userName: "월터",
      userEmail: "walter@gmail.com",
      gender: "MALE",
      accountCode: 10302,
      memberCode: 10402,
      provider: "LOCALS",
      "school": {
        "schoolName": "성남고등학교",
        "schoolGubun": "고등학교",
        "schoolLink": "http://www.sungnam.hs.kr",
        "className": "국어/언어",
        "classCode": 10202
      }
    },
  }}
/>



### `loginAccount` 
`Type` : (String) JWT 인증토큰정보 리턴 

> 로그인

`Input fields`
* input : <Link to="/input/account/#LoginAccountInput">LoginAccountInput</Link>

<GraphiQL
  query="
    mutation loginAccount($input: LoginAccountInput!) {
      loginAccount(input: $input)
    }
  "
  variables={{
    input: {
      userId: "tech@luxrobo.com",
      password: "1324qewr"
    }
  }}
/>



### `oauthLoginAccount` 
`Type` : (String) JWT 인증토큰정보 리턴 

> 소셜 로그인


`Input fields`
* input : <Link to="/input/account/#OAuthLoginAccountInput">OAuthLoginAccountInput</Link>

<GraphiQL
  query="
    mutation oauthLoginAccount($input: OAuthLoginAccountInput!) {
      oauthLoginAccount(input: $input)
    }
  "
 
  variables={{
    input: {
      userId: "tech@luxrobo.com",
      userName: "walter",
      userEmail: "tech@korea.kr",
      provider: "GOOGLE",
      accountCode: 10301,
      memberCode: 10401,
      accessToken: "ya29.a0AfH...",
      refreshToken: "mwdYxtKxXc..."
    }
  }}
/>


### `findAccountPassword`
`Type` : (ID) 

> 비밀번호 찾기

`Input fields`
* input : <Link to="/input/account/#FindAccountPasswordInput">FindAccountPasswordInput</Link>

<GraphiQL
  query="
  mutation findAccountPassword( $input:FindAccountPasswordInput! ){
    findAccountPassword( input: $input )
  }
  "
  variables={{
    input: {
      userId: "tech@luxrobo.com"
    }
  }}
/>



### `resetAccountPassword`
`Type` : (Boolean) 

> 비밀번호 리셋

`Input fields`
* input : <Link to="/input/account/#ResetAccountPasswordInput">ResetAccountPasswordInput</Link>

<GraphiQL
  query="
  mutation resetAccountPassword( $input:ResetAccountPasswordInput! ){
    resetAccountPassword( input: $input )
  }
  "
  variables={{
    input: {
      password: "1324qewr",
      authCode: "bf0171c0817a11eb96b202d1013e21f8"
    }
  }}
/>
