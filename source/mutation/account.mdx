---
title: 계정관리 Mutation
description: 계정관리 로그인/로그아웃등등 ...
---

import GraphiQL from '../../src/components/GraphiQL'
import { Link } from 'gatsby'


### `authAccountUserEmail`
`Type` : (ID) 

> 이메일 인증 요청하기

`Input fields`
* input : <Link to="/input/account/#AuthAccountUserEmailInput">AuthAccountUserEmailInput</Link>

<GraphiQL
  query="
  mutation authAccountUserEmail( $input:AuthAccountUserEmailInput! ){
    authAccountUserEmail( input: $input )
  }
  "
  variables={{
    input: {
      userEmail: "walter@gmail.com"
    }
  }}
/>


### `authCheckAccountUserEmail`
`Type` : (ID) 

> 이메일 인증여부 확인

`Input fields`
* input : <Link to="/input/account/#AuthCheckAccountUserEmailInput">AuthCheckAccountUserEmailInput</Link>

<GraphiQL
  query="
  mutation authCheckAccountUserEmail( $input:AuthCheckAccountUserEmailInput! ){
    authCheckAccountUserEmail( input: $input )
  }
  "
  variables={{
    input: {
      id: "1111",
      authCode:"11111111",
    },
  }}
/>

### `createAccountMember`
`Type` : (ID) 

> 회원가입

`Input fields`
* input : <Link to="/input/account/#CreateAccountMemberInput">CreateAccountMemberInput</Link>

<GraphiQL
  query="
  mutation createAccountMember( $input:CreateAccountMemberInput! ){
    createAccountMember( input: $input )
  }
  "
  variables={{
    input: {
      userId: "1111",
      password:"11111111",
      userName: "월터",
      userEmail: "walter@gmail.com",
      gender: "MALE",
      accountCode: 1,
      memberCode: 1,
      provider: "LOCALS",
    },
  }}
/>

### `loginAccount` 
`Type` : (String) JWT 인증토큰정보 리턴 

> 로그인


`Input fields`
* input : <Link to="/input/account/#LoginAccountInput">LoginAccountInput</Link>

<GraphiQL
  query="
    mutation loginAccount($input: LoginAccountInput!) {
      loginAccount(input: $input)
    }
  "
  variables={{
    input: {
      userId: "howto38",
      password: "11111111"
    }
  }}
/>


### `resetAccountPassword`
`Type` : (String) 

> 비밀번호 리셋

`Input fields`
* input : userEmail(String!)



<GraphiQL
  query="
  mutation resetAccountPassword( $input:ResetAccountPasswordInput! ){
    resetAccountPassword( input: $input )
  }
  "
  variables={{
    input: {
      userEmail: ""
    }
  }}
/>


### `oauthLoginAccount` 
`Type` : (String) JWT 인증토큰정보 리턴 

> 소셜 로그인


`Input fields`
* input : <Link to="/input/account/#OAuthLoginAccountInput">OAuthLoginAccountInput</Link>

<GraphiQL
  query="
    mutation oauthLoginAccount($input: OAuthLoginAccountInput!) {
      oauthLoginAccount(input: $input)
    }
  "
 
  variables={{
    input: {
      userId: "howto38",
      userName: "walter",
      userEmail: "walter@daum.net",
      provider: "GOOGLE",
      accountCode: 1111,
      memberCode: 2222,
      accessToken: "1212122",
      refreshToken: "we3e3432"
    }
  }}
/>