---
title: review Mutation
description: 리뷰 서비스 
---

import GraphiQL from '../../src/components/GraphiQL'
import { Link } from 'gatsby'

### `createReview`
> 리뷰 등록


`Input fields`
* input : <Link to="/input/review/#CreateReviewInput">CreateReviewInput</Link>

`Return Type`: <Link to="/object/review/#Review">Review</Link>

<GraphiQL
  query="
  mutation createReview( $input:CreateReviewInput! ){
    createReview( input: $input ) {
      id
      title
      comment
      serviceId
      serviceType
      replies {
        id
        comment
        reviewId
        createdAt
        updatedAt
      }
      createdAt
      updatedAt
    }
  }
  "
  variables={{
    input: {
      title: "리뷰제목",
      comment: "리뷰 내용입니다.",
      serviceId: "415551198",
      serviceType: "LESSON"
    },
  }}
/>

### `createReviewReply`
> 답변 등록


`Input fields`
* input : <Link to="/input/review/#CreateReviewReplyInput">CreateReviewReplyInput</Link>

`Return Type`: <Link to="/object/review/#Review">Review</Link>

<GraphiQL
  query="
  mutation createReviewReply( $input:CreateReviewReplyInput! ){
    createReviewReply( input: $input ) {
      id
      comment
      reviewId
      createdAt
      updatedAt
    }
  }
  "
  variables={{
    input: {
      comment: "답변내용",
      reviewId: "1"
    },
  }}
/>

